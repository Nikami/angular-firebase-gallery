import { DraggableDirective } from './drag-and-drop.directive';

describe('DraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableDirective();
    expect(directive).toBeTruthy();
  });
});
