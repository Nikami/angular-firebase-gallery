import { InputSelectionDirective } from './input-selection.directive';

describe('InputSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
