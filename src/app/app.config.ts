export enum LANG {
  EN = 'en'
}

export enum COOKIE {
  TOKEN = 'token'
}

export enum ROUTES {
  AUTH = 'auth',
  DEFAULT = 'gallery/categories',
  GALLERY = 'gallery',
  CATEGORIES = 'categories',
  UPLOAD = 'upload'
}
